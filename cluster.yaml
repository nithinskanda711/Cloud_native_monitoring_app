apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: cloud-native-cluster
  region: eu-west-1
  version: "1.36"
managedNodeGroups:
  - name: ng-1
    instanceType: t3.small
    desiredCapacity: 1
    minSize: 1
    maxSize: 1
    volumeSize: 20